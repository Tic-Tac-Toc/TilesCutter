<Window x:Class="TilesCutter.Options"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TilesCutter"
        mc:Ignorable="d"
        Title="Options" Height="114.607" Width="304.494" Loaded="Window_Loaded" Icon="Ritual.ico">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <RadioButton x:Name="jpg" Grid.Column="0" Content="JPG" HorizontalContentAlignment="Center"/>
        <RadioButton x:Name="gif" Grid.Column="1" Content="GIF" HorizontalContentAlignment="Center"/>
        <RadioButton x:Name="png" Grid.Column="2" Content="PNG" HorizontalContentAlignment="Center" IsChecked="True"/>
        <RadioButton x:Name="bmp" Grid.Column="3" Content="BMP" HorizontalContentAlignment="Center"/>

        <Button Grid.Column="3" Grid.Row="1" Content="Save" Click="Button_Click"/>
    </Grid>
</Window>
